{
   "version": "0.2.0",
   "configurations": [
      {
         "type": "lldb",
         "request": "launch",
         "name": "debug",
         "program": "${workspaceFolder}/bin/plunder.exe",
         "args": [],
         "cwd": "${workspaceFolder}"
      }
   ]
}
