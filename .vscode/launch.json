{
   "version": "0.2.0",
   "configurations": [
      {
         "type": "lldb",
         "request": "launch",
         "name": "debug",
         "program": "${workspaceFolder}/bin/clearicns.exe",
         "args": [],
         "cwd": "${workspaceFolder}"
      }
   ]
}
